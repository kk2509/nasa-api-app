<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>NASA Astronomy Picture of the Day</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>NASA Astronomy Picture of the Day</h1>
    </header>

    <main>
        <!-- Form to pick a date -->
        <form action="/search" method="POST" class="date-form">
            <input type="date" name="date" required>
            <button type="submit">View Picture</button>
        </form>

        <h2><%= apod.title %></h2>
        <p><%= apod.date %></p>

        <% if (apod.media_type === "image") { %>
            <img src="<%= apod.url %>" alt="Astronomy Picture of the Day">
        <% } else { %>
            <iframe src="<%= apod.url %>" frameborder="0" allowfullscreen></iframe>
        <% } %>

        <p><%= apod.explanation %></p>
    </main>

    <footer>
        <p>Powered by NASA API</p>
    </footer>
</body>
</html>
